extends ../../layouts/default

block content
	if !user
		#modal-auth(tabindex="-1", role="dialog", aria-labelledby="join", aria-hidden="true").modal-dialog
			.modal-content
				.modal-dialog
					+auth-forms({ modal: true, initial: 'join' })

block js
	script.
		jQuery(function($) {
			$('.auth-form').find('.form-control').eq(0).focus().select();
		});
